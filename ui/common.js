'use strict';

/*
function handle_auth_issues(res) {
    //if(!res) return;
    console.log("handle_auth_issue");
    console.dir(res);
    if(res.data && res.data.code) {
        var path = "/"+res.data.code;
        if(res.data.sub) {
            path += '?sub='+encodeURIComponent(res.data.sub);
        }
        console.log("recommended path: "+path);
        return path;
    }
}
*/
